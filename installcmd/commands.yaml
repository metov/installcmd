linux:
  debian:
    install: "sudo apt-get install"
    update: "sudo apt-get update"

  arch:
    install: "sudo pacman -S"
    update: "sudo pacman -Sy"

  manjaro:
    install: "sudo pacman -S"
    update: "sudo pacman -Sy"
