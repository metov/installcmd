linux:
  debian:
    install: "apt-get install"
    refresh: "apt-get update"

  ubuntu:
    install: "apt-get install"
    refresh: "apt-get update"

  arch:
    install: "pacman -S"
    refresh: "pacman -Sy"

  manjaro:
    install: "pacman -S"
    refresh: "pacman -Sy"
